Things to do
============

Expose gpsbabel options (-^3, -%1)

Easy notation for filtering data through gpsbabel

Make it possible to pass in filter and other options to read/write

Make read/write work with filehandles.

An iterator that allows modifications?

	$iter->insert_before($pt);
	$iter->insert_after($pt);
	$iter->replace_current($pt);

	$iter->delete_current();
	$iter->delete_before();
	$iter->delete_after();

