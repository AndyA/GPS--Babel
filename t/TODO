Things to test

gpsbabel invocation

all objects
    attr() - all modes
    all_points
    as_array

various accessor modes
    add() with single objects and collections
    append() with single objects and collection
    collection accessors - item for most objects, tracks, waypoints, routes for GPS::Babel::Data

iterators and filters

indexes

lookup by time
